<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MobileMG.StartingPageFlyout"
             Title="Flyout">
    <StackLayout>
        <StackLayout Orientation="Horizontal" BackgroundColor="#B4B4B4">
            <StackLayout Padding="15, 0">
                <Image WidthRequest="100" HeightRequest="150" Source="pine_tree"/>
            </StackLayout>
            <StackLayout HorizontalOptions="Start" VerticalOptions="CenterAndExpand">
                <Label x:Name="labelNomeUsuario" Text="nome-usuario"/>
                <Label x:Name="labelEmailUsuario" Text="email-usuario"/>

            </StackLayout>
        </StackLayout>
        <ListView x:Name="MenuItemsListView"
              SeparatorVisibility="None"
              HasUnevenRows="true"
              ItemsSource="{Binding MenuItems}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal" Padding="15,10" HorizontalOptions="FillAndExpand">
                            <Image Source="{Binding RowImage}" HorizontalOptions="Start" VerticalOptions="Center" Margin="0,0,15,0"/>
                            <Label VerticalOptions="FillAndExpand" 
                                   VerticalTextAlignment="Center" 
                                   Text="{Binding Title}" 
                                   FontSize="24"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>
